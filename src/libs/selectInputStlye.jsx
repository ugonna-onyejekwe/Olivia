export const selectInputStyle = {
  control: (styles) => ({
    ...styles,
    border: "1px",
    outline: "none",
    padding: "5px",
  }),
  singleValue: (styles) => ({
    ...styles,
    fontSize: "14px",
    fontWeight: "500",
  }),
  placeholder: (styles) => ({
    ...styles,
    fontSize: "14px",
    fontWeight: "500",
  }),
};
